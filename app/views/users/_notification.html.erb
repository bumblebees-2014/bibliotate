
<li class="dropdown open">
	<div class="notificationenvelope" data-note="yes"><i class="fa fa-envelope"></i><span class="caret"></span>
		<% if notifications.length >= 1 && notifications.first.status == true%>
		<i class="fa fa-plus" id="newnotify"></i>
		<% end %>
	</div>
</li>
